name: Hello world workflow 

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - run: ls -l
      - uses: actions/checkout@v4
      - run: ls -l
      - name: Greet the user 
        run: echo "Hello world!"
